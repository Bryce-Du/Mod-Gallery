<div class="ml-5 mt-5">
    <h1>Add a Moddable Game:</h1>
    <%= bootstrap_form_with(model: @game, local:true) do |f|%>
        <div class="row"><div class="col-2"><%= f.text_field :name %></div></div>
        <%= f.submit %>
    <% end %>
</div>